interface:
  display_name: "Signature Visual"
  short_description: "Search, direct, build, and verify a signature visual"
  default_prompt: "Use $signature-visual to find, direct, build, and verify one product-owned computational visual moment for this page."

policy:
  allow_implicit_invocation: true
